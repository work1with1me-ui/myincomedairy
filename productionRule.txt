rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Specifically users collection allow
    match /users/{userId} {
      allow read, write: if true;
    }
  }
}


// devlopment mode rule 

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
